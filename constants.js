const COLS = 10;
const ROWS = 20;
const BLOCK_SIZE = 30;
//This is the size in px;

const KEY = {
  //This is the code of the keys in the keyboard
  LEFT: 37,
  RIGHT: 39,
  DOWN: 40,
  SPACE: 32
};
Object.freeze(KEY);
